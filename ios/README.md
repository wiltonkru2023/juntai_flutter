A pasta nativa iOS deve ser gerada pelo Flutter SDK:

flutter create . --project-name juntai --org app.juntai --platforms android,ios

Depois adicione GoogleService-Info.plist, Maps API key, Sign in with Apple e Push Notifications.
